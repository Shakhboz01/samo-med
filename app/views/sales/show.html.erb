<script>
// if (!localStorage.getItem("SellpageReloaded")) {
//      // Reload the page
//       localStorage.setItem("SellpageReloaded", "true");
//       location.reload();
//     } else {
//       // Clear the flag for subsequent reloads
//       localStorage.removeItem("SellpageReloaded");
//     }
</script>

 <div class='mx-2'>
  <h2 class='text-center'>Sotuv bo'limi</h2>
  <div class="text-centerjustify-content-between align-items-center">
    <h3 class="card-title text-center font-weight-bold" id='pack-name'></h3>
  </div>
  <div class='row'>
    <% unless @sale.closed? || params.dig(:prepayment) %>
      <div class='col-sm-12 col-md-3'>
        <%= render 'product_sells/form', product_sell: @product_sell, rate: @rate %>
      </div>
    <% end %>
    <div class='col-sm-12 col-md-7'>
      <% unless @product_sells.count.zero? || params.dig(:prepayment) %>
        <%= render 'product_sells/product_sells', shrink: true, product_sells: @product_sells, rate: @rate %>
      <% end %>
    </div>
    <% unless @sale.closed? %>
      <div class='col-sm-12 col-md-2'>
        <%= render 'sales/form', sale: @sale %>
      </div>
    <% end %>
  </div>
</div>


<script>
  $(document).ready(function() {
    $("input").focus(function() {
      $(this).select();
    });
  });
</script>

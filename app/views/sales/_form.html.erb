<div class='p-3' style='color: black; box-shadow: rgba(0, 0, 0, 0.35) 0px 5px 15px;'>
  <%= simple_form_for(sale) do |f| %>
    <%= f.error_notification %>
    <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>
    <%= f.input :status, as: :hidden, input_html: { value: 1 } %>

    <div class="form-inputs">
      <%= f.label :price_in_usd, 'Dollarda' %>
      <%= f.input :price_in_usd, label: false %>
      <%= f.input :total_price, label: false, disabled: true, as: :hidden, input_html: {id: 'total-price'} %>
      <%= f.association :buyer, label: 'Mijozni tanlang', collection: Buyer.active.order(weight: :desc).order(name: :asc), include_blank: false %>
      <%= f.input :total_paid, label: params.dig(:prepayment).present? ? 'цена выручки' : 'Mijoz to\'lagan summa' %>
      <%= f.input :payment_type, label: 'To\'lov turi', collection: Sale.payment_types.keys.map { |unit| [Sale.human_attribute_name("unit.#{unit}"), unit] }, include_blank: false %>
      <%= f.input :comment, label: 'Kommentariya' %>
      <%= f.button :submit, 'Sotuvni yakunlash' %>
    </div>
  <% end %>
</div>

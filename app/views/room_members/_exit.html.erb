<% if room_member.active_member %>
  <%= simple_form_for(room_member) do |f| %>
    <%= f.input :active_member, label: false, as: :hidden, input_html: {value: !room_member.active_member} %>
    <%= f.button :submit, "Palata №#{room_member.room.name} dan chiqarish", class: 'btn-danger' %>
  <% end %>
<% end %>

<div class="card container my-5">
  <div class="card shadow-lg">
    <div class="card-header d-flex justify-content-between align-items-center">
      <h3 class="card-title font-weight-bold">Зарплаты/Аванс</h3>
      <%= link_to 'Добавить новый', new_salary_path, class: "btn btn-success min-width-btn" %>
    </div>

    <div class="card-body">
      <table class="table table-hover table-striped">
        <thead class='table-dark'>
          <%= render 'salary_data' %>
          <tr>
            <th><%= sort_link @q, :id, 'ID' %></th>
            <th><%= sort_link @q, :month, 'Sana' %></th>
            <th>Kimga</th>
            <th><%= sort_link @q, :prepayment, 'Avansmi?' %></th>
            <th><%= sort_link @q, :price, 'Qancha?' %></th>
            <th><%= sort_link @q, :payment_type, 'tolov turi' %></th>
            <th colspan="3"></th>
          </tr>
        </thead>
        <tbody>
          <% @salaries.each do |salary| %>
            <tr>
              <td><%= salary.id %></td>
              <td><%= salary.month %></td>
              <td><%= salary.team&.name || salary.user&.name %></td>
              <td><%= active_check(salary.prepayment) %></td>
              <td><%= num_to_usd(salary.price) %></td>
              <td><%= salary.payment_type %></td>
              <th><%= clink_to 'Редактировать', edit_salary_url(salary), 'bi-pencil' %></th>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
    <%= page_entries_info @salaries, entry_name: "Зарплат/Аванс" %>
    <%= paginate @salaries %>
  </div>
</div>
